import { LightningElement } from 'lwc';
import footer from '@salesforce/resourceUrl/footer'

export default class Footer extends LightningElement {
    links = ['Home', 'Books', 'Contact', 'Information', 'Facebook', 'Instagram']

}